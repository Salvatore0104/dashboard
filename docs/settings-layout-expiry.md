# 系统配置布局与项目过期规则

> 历史阶段记录：项目到期清空组织成员规则已被后续发布替代。当前归档项目退出同步，保留现有组织和成员；见 [README](../README.md) 与 [当前发布记录](access-production-release-20260922.md)。

日期：2026-09-22。分支：stage/11-settings-layout-expiry。

- 使用明确的跨列类替代 nth-child 排版；基础设置横排，凭据卡片并排，卡片内容顶部对齐。同步开关、小时周期和保存按钮紧凑排列；600px 以下转单列，颜色选择器自适应换行。
- 项目结束日期早于本地当天时，有效项目成员列表为空，即使人员排期仍未结束。结束日期当天仍有效。
- 同步仅移除该项目管理的成员关系，保留项目、组织绑定和原组织 ID；不删除 wowidea 用户或其他组织关系。项目延期后可以重新加入成员。
- 明确删除项目仍走既有删除流程；到期不等于删除。自动同步关闭时需要手动同步。

验证：50 项测试通过，新增用例覆盖项目过期但人员排期未结束、全局清空、保留组织与绑定、重复定时同步和延期后重新加入。浏览器验证收起/展开编辑、正常宽度和 390px 窄屏无卡片横向溢出。本地服务已重启，健康检查 200，调度仍关闭。未对真实平台执行到期清理测试，逻辑测试使用隔离数据库和模拟适配器。

仅修改 Dashboard；使用 frontend-design 指导紧凑布局、computer-use 完成页面检查。未部署生产。回滚基线 aa03673，可撤销本阶段提交后重启本地服务，不需回滚数据库。
