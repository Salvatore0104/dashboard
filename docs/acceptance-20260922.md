# Dashboard 全功能验收记录（进行中）

主任务负责页面验收与最终合并；修复任务 `01a0c6b8-ae3f-7761-950c-073acdf7ec96` 负责 `stage/08-acceptance-fixes`。未完成全部真实验收前不合并 main。

## 目标

1. 新增和删除项目同步新增和删除对应组织。
2. 排期到期后自动退出对应项目组织，保留其他组织关系。
3. 新增项目成员自动加入对应组织。
4. 后台按钮可用，错误不能伪报成功；全局同步当前结果不混入历史，历史按需加载。
5. 组织名称等于项目名称，不自动添加前缀。
6. 修复有测试、Git 提交与推送，全部验收后才合并 main。

## 首轮真实页面检查

环境：本地 `http://127.0.0.1:5000`，平台 `https://wowidea.top`。本轮未执行线上组织删除、成员移除或全局同步写入。

| 项目 | 观察结果 | 状态 |
| --- | --- | --- |
| 后台首页 | 2 个项目、31 名人員，页面可打开 | 通过入口检查 |
| 绑定人员入口 | 独立弹窗有“一键绑定全部”，31 条 auto_matched 记录 | 通过显示检查 |
| 一键绑定预览 | 点击返回 400；请求 JSON 为“项目不存在” | 失败，旧进程未加载新路由 |
| 全局同步预览 | 2 个当前项目 + 6 个历史删除项，合计错误显示 8 项 | 失败，运行旧代码 |
| 查看同步日志 | 404 NOT FOUND | 失败，运行旧代码 |
| 添加项目空名称 | 不提交，显示“请输入项目名称” | 通过 |
| 编辑项目 | 正确回填名称和起止日期，取消无保存 | 通过入口检查 |
| 出差配置 | 正确回填项目与日期，未启用时人员列表隐藏 | 保存待验收 |
| 请假编辑 | 人员、类型和日期控件可用，取消无保存 | 保存及同步待验收 |
| wowidea 测试连接 | 页面显示“连接成功，读取到 2 个组织” | 通过（组织为根节点数量） |
| 钉钉测试连接 | 页面显示“连接成功” | 通过 |
| 获取钉钉人员 | 加载 31 人，不含平台绑定内容 | 通过读取检查 |
| 前台人员布局 | 前期约 1/3、后期约 2/3；第二行四部门；隐藏人员不出现 | 基本布局通过，页面横向溢出待修复 |
| 平台组织树 | 执行项目组下有两个 Dashboard 前缀组织，各 2 名成员 | 归属结构可见，前缀待修复 |

## 代码审查已发现的问题

- `project_members` 无当前有效排期时回退到全部历史排期，导致过期成员仍加入同步。
- 没有完整成员移除流程，项目删除只保留 pending_delete。
- 分钟级幂等键可能吞掉同一分钟内的成员变更。
- 需验证批量接口部分失败、所有同步入口互斥、实时 assignment 同步与调度状态。
- 导出 JSON 直接返回配置表，需排除凭据等敏感配置。
- 多个保存/删除按钮没有检查 HTTP 和业务失败结果，仍显示成功。
- 完整 Swagger 已由修复任务确认组织 DELETE 接口；早先“平台无删除接口”的推断不成立。

## 后续验收要求

修复后重启本地旧进程，再完成：无前缀测试项目创建、名称更新、当前成员新增、同一分钟变更、到期移除、项目删除对应组织清理、平台其他组织关系保持、重复执行无重复项、当前与日志隔离、配置保存与脱敏导出、失败提示、调度单轮与真实开关。真实测试只使用明确记录 ID 的验收项目；不清理非 Dashboard 组织。

现有 28 项单测通过仅代表初步修改，不代表上述流程已经通过。

## 第二轮代码审查

提交 `0a122b8` 已产生，尚未通过主审：成员指纹永久缓存导致 A→退出→A 或失败重试被阻断；目标项目删除调用全局同步；未知批量返回可能伪报成功；孤儿结果与当前结果仍未完全分离。已要求修复对话继续修正，禁止据此合并 main。

独立验收测试任务：`01a0c6c3-d8af-7243-9652-49f22185a824`，只使用隔离临时数据库与 mock 平台，负责 `test_lifecycle_acceptance.py`。

真实本地数据基线：2 项目、31 人员、6 排期。SQLite 一致性备份已创建：`acceptance-backup-20260922-093909.db`（本地忽略文件，不提交；仅为本地数据库回滚，不替代平台备份）。未对既有线上组织和成员做验收写入。

## 本地服务重启后复测

已停止旧 PID 30496，用项目 `.venv` 的 Python 启动 `app.py`（启动 PID 24912），日志为本地忽略文件 `server-acceptance.out.log` 和 `server-acceptance.err.log`，项目 scheduler 显式保持关闭。

- 实际页面全局预览只显示 2 个当前项目，历史 6 项不再混入，2 项待去前缀改名。
- 实际页面日志窗口正常显示“暂无全局同步日志”，HTTP 200，不再 404。
- 一键绑定实际页面确认框成功显示 31 人，无 400；点击确认执行后 API 回读 31/31 已关联。预览将既有 auto_matched 计为新增仍需修正。
- 全套 38 项测试通过，包括新增独立生命周期测试；仍需最终稳定代码上复跑。
- 实际页面仍残留“离开成员仅标记待移除”旧提示，已要求修复。
- 尚未验证：真实平台项目/组织创建删除、成员到期移除及再次加入、全局执行历史清理、全部保存按钮、调度运行和最终合并。

## 真实测试项目创建

平台手工备份已确认：`6ab1e080cd465762a7b555bc`，时间 `2026-09-22T01:57:20.180Z`，服务器位置 `/app/backups/mongodb/mongodb_backup_20260922095614_SKEUvX7c.gz`。创建请求客户端超时，但查备份列表确认完成，未重复提交。

Computer Use 在后台添加并保存 `同步验收20260922-0958`，项目 ID `1790042287207`，平台组织 ID `6ab1e0b0cd465762a7b55679`。页面显示已绑定，真实 API 回读确认名称完全相等、无自动前缀、同名组织仅 1 个、父 ID `6ab0eb2ccd465762a7adbaf7` 对应执行项目组、初始成员为 0。

该项目为本轮验收创建，成员/到期/删除测试尚待继续；最终需清理这个明确 ID 的测试项目及其组织，不触碰其他组织。

### 成员与删除真实 API 验收完成

使用一名已有稳定 ID 关联人员，仅增加本次测试项目排期 `1790042487645-3e68fce2`。POST assignments 自动同步返回 added=1；平台回读原部门关系保留，测试组织新增。PUT 将排期结束日期改为 2026-09-21 后自动返回 removed=1，平台回读只剩原组织；恢复 2026-09-22 有效排期后 added=1，再次入测试组织。真实批量响应是 success 对象数组（user_id）与 failed 数组，解析成功。

DELETE 本次项目 `1790042287207` 返回 cleanup.status=deleted、removed=1，平台回读确认测试组织 `6ab1e0b0cd465762a7b55679` 已不存在，测试人员仅保留原部门关系。数据库回归 2 个原项目、6 条原排期、31 人；两个原 Dashboard 组织保留。

这轮创建使用 Computer Use，成员变更及删除通过本地业务 API 验收，并以平台 API 独立回读；尚不等于前台拖拽及定时到期触发验收。真实调度、全局执行与剩余后台按钮仍待验证。

### 全局同步真实接口验收

事先只读确认 6 个孤儿绑定在平台均不存在（其中部分从未创建组织）。首次全局 run 返回 success=true、当前 projects=2、cleanup=6、added=0、removed=0、existing=4；两个现有项目完成去前缀改名。再次 preview 返回 rename=0、projects=2；再次 run 返回 success=true、cleanup=0、added=0、removed=0。独立日志接口返回 2 条运行记录，历史不会重新进入当前项目结果。

页面已验证准确预览与新提示，但点击原生 confirm 后 IAB 控制出现 Emulation.setFocusEmulationEnabled 超时；确认后端未执行（logs=0）后才使用业务 API 验收。已交修复对话将全局同步与批量绑定改为页面内确认，完成后仍需 GUI 复测。此问题尚未视为通过。

### 调度循环与配置保存

新增独立测试实际调用 `schedule_project_sync` 循环，替换等待函数以运行一整轮后退出：首项目模拟失败后下一项目仍成功写 scheduler 运行记录；后续一轮正确移除到期成员并记录 removed_count=1。默认关闭不会启动线程；10 分钟间隔解析为 600 秒。独立测试目前 9 项通过。此为隔离调度循环证据，未宣称线上已连续运行十分钟。

Computer Use 实际点击系统配置“保存配置”，页面提示“配置已保存”。未修改输入值，保持现有设置。

### 页面内确认复测（10:18）

- 修复提交 `6bcd343` 已推送，批量绑定和全局同步使用页面内确认；取消能关闭确认层并返回原窗口。
- 本地后端旧 PID 4220 未加载最新绑定统计；停止后通过项目虚拟环境重新启动，项目调度继续关闭。预览现在准确显示总计 31、新增 0、已有 31、候选/冲突/未匹配均 0。实际点击批量绑定确认，窗口正常返回，无原生确认超时。
- 全局同步 GUI 预览只有当前 2 个项目，改名/新增/移除/清理均 0，已有成员 4。实际执行后独立日志显示 10:18:33 succeeded，当前结果没有混入历史记录。
- 新发现：执行后结果表缺少项目与组织名称，仅显示已绑定及成员统计。已交修复任务继续修正，尚未验收通过。
- 完整隔离测试复跑 40 项通过。仍待完成前台成员操作、出差/请假/显示开关保存、导出及真实调度触发等验收，不据此合并 main。
- 前台显示开关 GUI 切换后 SQLite 读回为 1，再切回并读回为 0，已恢复原设置。
- CSV 导出捕获到真实下载事件。JSON 导出接口 HTTP 200/success=true，敏感配置键未包含；但按钮直接导航到 JSON，没有下载事件，已交修复任务处理。
- 已有请假记录回填正确，不改变原值点击保存后页面提示“请假记录已保存”。清空与重新设置流程尚待专门测试数据验收。
- 代码发现项目本地删除成功但外部组织清理失败时，前端只提示“项目已删除”；已要求修复清理失败/待重试反馈，并替换删除原生确认以支持完整 GUI 验收。

### 第二个 GUI 验收项目（尚未清理）

- 页面创建 `界面验收20260922-1025`，项目 ID `1790043924016`，组织 ID `6ab1e715cd465762a7b5aa4a`；页面显示同名已绑定，无前缀。
- 页面启用出差，选择一名测试人员并保存，页面提示成功，数据库回读 enabled=1、人员数=1。仅作用于本次测试项目。
- 页面编辑开始日期至 2026-09-20 并保存成功，目的是将测试项目排至第一行，方便前台拖拽。
- 前台出现布局回归：甘特容器横向滚动改变 sticky 参照后，日期头保留导航栏 60px 偏移，遮住首个项目行。已交修复任务处理；拖拽尚未通过。该测试项目及组织仍存在，后续必须继续验证并按准确 ID 清理，不能误报基线已恢复。

### 第三轮真实 GUI 和调度复测

- `eb638a3` 修复首行遮挡，截图确认首项目完整可见。Computer Use 从人员池拖拽测试人员到测试项目当天，产生一条当日排期；平台独立回读测试组织成员存在、原部门关系保留。
- 仅将该测试项目的唯一排期调整为昨日，不通过实时同步接口，以验证 scheduler 自身。实际调用 `schedule_project_sync`，仅跳过等待并限制只同步该测试项目，一轮后停止。数据库记录 trigger=scheduler、status=succeeded、removed_count=1、error_count=0；平台读回到期成员已移除，原部门关系保留。长期调度仍关闭，未宣称已经开启十分钟持续运行。
- GUI 删除测试项目 `1790043924016`，页面内确认明确项目名；独立回读组织 `6ab1e715cd465762a7b5aa4a` 不存在，本地恢复 2 项目/31 人/6 排期。前一节“尚未清理”现已由本段关闭。
- `d9a45f7` 兼容下载复测捕获到真实 JSON download 事件，页面 URL 保持 admin.html；此前 Blob 方案在 IAB 没有下载，已替换。CSV 下载此前已通过。
- 全局 GUI 执行结果保留两个当前项目名称，历史不混入，待清理=0。隔离测试共 42 项通过，admin.js/board.js 语法和 diff 检查通过。
- 手动同步请假按钮执行后同步时间更新；临时本地人员 `acceptance-local-person-20260922` 的新建请假保存、清空均显示成功。该临时人员尚待 GUI 删除，未向平台创建用户或组织。
- 临时人员已通过页面内确认删除，明确显示姓名，页面返回“人员已删除”和 31 人；本轮临时人员清理完成。
