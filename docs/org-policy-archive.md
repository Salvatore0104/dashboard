# 新组织默认值、保留组织与自动归档

2026-09-22，分支 `stage/12-org-policy-archive`，回滚代码基线 `aefe3b6`。

## 当前规则（取代之前文档中的组织删除规则）

- 新组织直接挂在执行项目组下。创建请求一次性设置 `balance: 5000` 和 `balance_deduction_strategy: organization_first`。
- 管理创建接口为 `POST /organization`。当前线上 OpenAPI 文档、组织读取接口与前端 `DjlRsr1_.js` / `BukS5gFy.js` 已核实字段和管理创建路径。
- 已绑定组织不补充值、不重置余额。创建超时后的同名组织检查仍阻止重复创建，不自动认领同名组织。
- 看板的组织删除适配器直接拒绝执行，无组织 DELETE 请求。删除本地项目只移除其托管成员，并将绑定标记为 retained，保留组织和余额。重试、全局同步与一致性检查识别 retained。
- 有余额不阻止成员清理；仍验证组织归属和 Dashboard 标记。
- 项目结束日期早于本地当天时自动进入归档区，默认折叠；主项目列表仅显示待执行、执行中。页面跨天每 30 秒检查一次。延期后自动返回活动列表。
- 项目归档不删除项目、排期或平台组织。成员清理发生在手动或定时同步时。原有组织不回填 5000。

## 验证

- 53 项 Python 测试通过；语法和 diff 检查通过。
- 新测试验证初始化字段、单次请求、余额消费后重复绑定不重置、有余额时可清理成员；删除测试验证没有组织删除调用及 retained 状态。
- 5001 独立数据库浏览器验证：执行中/待执行共 2 项，归档 1 项；到期后活动 1 项、归档 2 项，归档可展开并编辑。
- 5000 本地服务已重启，真实页面可见折叠归档区。
- 真实测试项目：`stage12-acceptance-20260922`；组织：`6ab2032fcd465762a7b66f69`；名称 `[TEST][dashboard-local] stage12-policy-20260922`。
- 平台回读：父组织 `6ab0eb2ccd465762a7adbaf7`，余额 5000，策略 organization_first，成员 0。重复绑定与到期同步后组织和余额仍保留。
- 保留测试项目在归档区，测试组织未删除。清理由平台管理员按上述确切组织 ID 管理。

平台备份 ID：`6ab20314cd465762a7b66ee1`，服务端路径 `/app/backups/mongodb/mongodb_backup_20260922122345_-P_i7MHt.gz`。备份请求超时后回查确认完成，没有重复创建备份。

回滚可撤销本阶段提交并重启 Dashboard。数据库整库恢复需另外确认；平台测试组织与积分不得自动删除/回收。未修改平台代码或部署，没有模型付费调用。
