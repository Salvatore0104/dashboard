# 同名组织复用与日期描述

2026-09-22，stage/13-reuse-org-dates，代码回滚基线 ed96132。

- 在执行项目组下发现唯一同名组织时绑定已有 ID，不执行创建、不写余额或扣费策略。多个同名、父组织不符、已绑定其他有效项目会报冲突。
- 已删除且已完成成员清理的本地旧绑定可以让出组织 ID，允许新项目复用平台保留组织；同步记录不删除。
- 新建和复用组织的描述使用项目起止日期 YYYYMMDD-YYYYMMDD。修改日期时更新描述；既有项目同步时也补齐描述。仅 PATCH description，不携带财务字段。
- 新建组织仍一次性初始化 5000 和 organization_first。看板仍禁止删除组织。
- 成员清理依赖持久化组织 ID、父组织和托管成员 ID；描述不再用作归属口令，非托管成员不移除。

验证：56 项测试、Python 编译和 diff 检查通过。覆盖创建描述、复用保留财务字段、延期描述、旧绑定让出 ID、重名拒绝、日期编辑接口。

真实验收使用已有测试组织 6ab2032fcd465762a7b66f69，没有新增组织。通过隔离本地映射执行同名关联并禁止创建调用，回读 ID 不变、余额和策略不变，描述为 20260920-20260921（该测试项目实际日期）。随后更新其本地描述缓存。未改其他真实项目。

写入前备份：6ab20582cd465762a7b678b8，路径 /app/backups/mongodb/mongodb_backup_20260922123408_Is3D9HPo.gz。仅需描述回滚时可按确切组织 ID 恢复此前描述，不需整库恢复；整库恢复另行确认。

本轮用户授权验证后合并 main。包含此前 stage09–12 已完成变更，不部署生产 Dashboard。
