# 配置隐藏与按小时组织同步验收

日期：2026-09-22。分支：`stage/10-private-config-scheduler`。

## 行为

- 钉钉和 wowidea 配置默认收起，仅显示配置状态、固定星号和编辑按钮。
- 编辑时字段为空；留空保存保留已有配置，保存或取消后清空输入并收起。
- 配置读取接口不返回钉钉凭据、wowidea 账号、密码或令牌。钉钉测试和读取人员由服务端读取凭据。
- 后台可开启/关闭组织自动同步，周期为 1–168 整数小时。保存持久化并唤醒现有工作线程，重新安排下一次运行，无需重启。
- 看板用简短组织状态、暂停图标或下次时间展示，完整说明放在悬浮提示中。前台及 TV 不展示人员绑定标记。

## 验证

- 49 项 Python 测试通过；Python 编译、两个修改过的 JS 语法检查及 git diff 检查通过。
- 浏览器验证两组配置编辑为空、空值保存保留原配置、保存后隐藏。
- 已保存的钉钉和 wowidea 凭据连接测试成功，无需浏览器提供已保存的凭据。
- 临时启用一小时周期，确认运行时产生下一次执行时间，前台显示简短时间。
- 验收后恢复自动同步关闭；没有执行自动同步或更改平台组织数据。

## 回滚

代码基线为 `f4f9729`。如需回滚，可撤销本阶段提交并重启本地 Dashboard；不要重置用户数据库。调度开关可随时在系统配置中关闭。

本阶段仅修改本地 Dashboard，未部署生产；权限设计在独立任务讨论。
